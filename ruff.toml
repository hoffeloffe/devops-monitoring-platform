target-version = "py38"
line-length = 100

[lint]
# Default ruff rules (pyflakes + a subset of pycodestyle) plus bugbear.
# Kept intentionally focused so the check stays meaningful and low-noise.
select = ["E4", "E7", "E9", "F", "B"]
